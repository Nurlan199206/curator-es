elasticsearch:
  client:
    hosts:
      - http://127.0.0.1:9200
  other_settings:
    username: elastic
    password: password
logging:
  loglevel: INFO
  logfile: /var/log/curator
  logformat: default
